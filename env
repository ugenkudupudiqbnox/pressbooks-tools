# env

DB_NAME='pressbook'
DB_USER='wpuser'
DB_PASSWORD='yourpass'

# Optionally, you can use a data source name (DSN)
# When using a DSN, you can remove the DB_NAME, DB_USER, DB_PASSWORD, and DB_HOST variables
# DATABASE_URL='mysql://database_user:database_password@database_host:database_port/database_name'

# Optional database variables
DB_HOST='localhost'
DB_PREFIX='wp_'

HTTP_HOST=pressbook.braou.ac.in
DOMAIN_CURRENT_SITE="${HTTP_HOST}"

WP_ENV='production'
WP_HOME="https://${HTTP_HOST}"
WP_SITEURL="${WP_HOME}/wp"

# Specify optional debug.log path
WP_DEBUG_LOG='/var/log/wordpress.log'

# Generate your keys here: https://roots.io/salts.html
AUTH_KEY='yoursalt'
SECURE_AUTH_KEY='yoursalt'
LOGGED_IN_KEY='yoursalt'
NONCE_KEY='yoursalt'
AUTH_SALT='yoursalt'
SECURE_AUTH_SALT='yoursalt'
LOGGED_IN_SALT='yoursalt'
NONCE_SALT='yoursalt'

# Pressbooks specific env variables
# DOCRAPTOR_API_KEY='your_docraptor_api_key' # Uncomment this line if you are using Docraptor for PDF generation
PB_PRINCE_COMMAND='/usr/bin/prince' # Uncomment this line if you are installing PrinceXML on your server
# PB_MATHJAX_URL= 'https://localhost:3000/' # Uncomment and set this line if you are self-hosting Pressbooks MathJax
